#include <bits/stdc++.h>
using namespace std;

// Rearrange the array in alternating positive and negative items
// Brute :

// f()...

int main() {
    int n;
    cin >> n;
    vector<int> nums(n);
    for (int i = 0; i < n; i++) {
        cin >> nums[i];
    }

    // ...
}

// Time-complexity  : O()
// Space-complexity : O()