#include <bits/stdc++.h>
using namespace std;

// Find missing number in an array

int main() {
    int n;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    // write code here
}